let quantidade = Number(prompt(`Digite quantas vezes você quer repetir essa mensagem?`))
while(quantidade <10) {
    console.log("Mensagem")
    quantidade++ //soma 1
}

let contador = 0
let quantidade = Number(prompt(`Repetir quantas vezes?`))
while(contador < quantidade) {
    console.log("Mensagem "+ (contador+1))
    contador++ //soma 1
}

